apiVersion: v1
kind: Service
metadata:
  name: dev-io-mcp
  labels:
    app.kubernetes.io/name: dev-io-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dev-io-mcp
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
